# Core LlamaIndex (Python 3.14 compatible — use core directly, NOT the metapackage)
llama-index-core>=0.12.0
# NOTE: llama-index-retrievers-bm25 is intentionally excluded —
#       it depends on pystemmer which needs C++ Build Tools on Windows.
#       BM25 is implemented directly with rank_bm25 (pure Python) in retriever.py.

# Vector database — direct client (avoids llama-index-vector-stores-qdrant plugin)
qdrant-client>=1.12.0

# LLM — direct Google GenAI SDK
google-genai>=2.7.0

# BM25 — pure Python, no compilation required
rank-bm25>=0.2.2

# Reranking disabled in lite branch

# FastAPI
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# PDF parsing
pypdf>=4.3.1
pdfplumber>=0.11.2

# Utils
python-multipart>=0.0.9
python-dotenv>=1.0.1
pydantic>=2.8.2
pydantic-settings>=2.3.4
httpx>=0.27.0
numpy>=2.0.0

# Production Server
gunicorn>=23.0.0
